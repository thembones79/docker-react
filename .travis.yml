language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t szumnarski/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true szumnarski/docker-react npm run test
